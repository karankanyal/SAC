function Categories() {
  const categories = [
    "Temple",
    "Oil",
    "Digital",
    "Acrylic",
    "Canvas",
    "Sketch",
  ];

  return (
    <section>
      {/* {categories.map((category, index) => (
        <div key={index}>
          <h3>{category}</h3>
          <img alt={category} />
        </div>
      ))} */}
    </section>
  );
}

export default Categories;
